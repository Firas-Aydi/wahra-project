export interface Pierre {
  id: string; // Identifiant unique
  name: string; // Nom de la pierre (ex : Améthyste)
  description: string;
  bienfaits: string;
  image: string;
  categoryId: string[];
}
